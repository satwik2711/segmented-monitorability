[project]
name = "segmented-monitorability"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "huggingface-hub>=1.0",
    "numpy>=2.0",
    "scipy>=1.15",
    "torch>=2.6",
    "transformers>=5.0",
]

[tool.uv]
package = false
